字符串可迭代的
for循环：
   for变量in可迭代的东西：
      代码
      把可迭代的东西中每一项的内容拿出来，挨个赋值给变量，每一次赋值都要执行一次循环体（代码）

for循环想要计数，必须借助range
range（n）：从0数到n，不包含n
range（m，n）：从m数到n,不包含n
range(m,m,s)：从m数到s，不包含n，每次间隔是s
平时用的多的是for循环，while循环多用的是死循环（无线循环
）